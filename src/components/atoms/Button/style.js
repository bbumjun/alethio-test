import styled from 'styled-components';

export const Button = styled.button`
  padding: 0.5rem;
  border: solid black 0.1rem;
  border-radius: 5px;
  background: none;
  &:hover {
    cursor: pointer;
  }
`;
